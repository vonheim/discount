<html>
  <head>
    <script type="text/javascript" src="script/canvasjs.min.js"></script>
  </head>
  <body>


  <% _.each(nodestat.widgets, function(widget, widgetIndex) { %>
    <% if( widget.name ) { %>
      <h2><%= widget.name %></h2>
    <% } %>

    <%- widget.process(nodestat.stat(), widgetIndex) %>

  <% }); %>


  </body>
</html>

